import { Stack } from "expo-router";
import { View } from "react-native";

const Layout = () => {
    return <View style={{height: '100%', fontFamily: 'Poppins'}}><Stack /></View>
}

export default Layout;